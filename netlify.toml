[build]
  command = "npm run build"
  publish = "dist"

# Proxy API calls to AWS backend
[[redirects]]
  from = "/api/*"
  to = "https://app.bestrongagain.com/api/workout/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
